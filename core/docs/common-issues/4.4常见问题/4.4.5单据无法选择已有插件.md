### 问题描述
单据注册脚本插件时，无法选择已上传的脚本插件

### 问题分析
脚本插件支持很多场景，如单据、报表、调度、流程等，单据插件基类是 `AbstractBillPlugIn`， VSCode 创建插件时模板默认使用了动态表单插件 `AbstractFormPlugin`，与单据插件基类不匹配，故在单据选择已有插件时列表无法显示。

### 解决办法
将已有插件基类改为对应业务场景的插件基类，如单据基类请选择 `AbstractBillPlugIn`。